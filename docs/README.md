# Documentation Index

`algorithm/` contains the RF detection and firmware-integration notes copied
from the current Z6 workspace. `reference/` contains supporting project notes,
including the display reset configuration and repository-snapshot record.

Firmware build, flashing, and verification instructions are kept beside the
Solution in `../firmware/` because the scripts use paths relative to that
directory.
