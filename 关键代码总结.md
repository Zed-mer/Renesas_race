# IMU_V9 关键代码总结

本文对当前工程中的关键代码模块进行归纳，便于后续撰写论文、制作答辩 PPT 或绘制软件流程图。当前工程以双 IMU 姿态采集为基础，完成上臂与前臂姿态解算、T-Pose 固连校准、相对运动学求解、关节角映射、串口通信以及机械臂舵机控制。

## 1. 总体入口与主流程

### 1.1 系统入口

- 文件：`src/hal_entry.c`
- 关键函数：`hal_entry()`

功能说明：

- 作为裸机主入口函数；
- 上电后直接调用 `imu_test()` 进入 IMU 主业务流程；
- 不使用 RTOS，系统全部逻辑在单线程主循环中完成。

### 1.2 主控制函数

- 文件：`zed/applications/app_uart_test.c`
- 关键函数：`imu_test()`

功能说明：

- 初始化串口 `UART7`；
- 初始化微秒时间基；
- 初始化上下两枚 ICM42688；
- 打开数据就绪中断 `IRQ8/IRQ9`；
- 完成上下臂陀螺零偏采样；
- 启动标定状态机；
- 打开按键中断 `IRQ6`；
- 初始化机械臂舵机链路；
- 进入实时主循环。

主循环的核心工作包括：

- 读取上下臂 IMU 数据；
- 温漂补偿；
- 静止尖峰抑制；
- Mahony 姿态融合；
- 处理按键标定事件；
- 处理串口标定命令；
- 若标定完成，则解算机械臂关节角并输出。

## 2. 中断与数据采样链路

### 2.1 数据就绪中断

- 文件：`zed/applications/app_uart_test.c`
- 关键函数：`icu8_callback()`、`icu9_callback()`

功能说明：

- `icu8_callback()` 对应上臂 IMU；
- `icu9_callback()` 对应前臂 IMU；
- 中断中不直接读取传感器，而是调用 `imu_mark_data_ready()` 记录“有新数据到达”。

这样设计的优点是：

- 中断程序短小；
- 实际运算仍在主循环中完成；
- 便于保持姿态更新链路稳定。

### 2.2 按键中断

- 文件：`zed/applications/app_uart_test.c`
- 关键函数：`botton6_callback()`

功能说明：

- 仅置位 `button_pending` 标志；
- 主循环中的 `imu_handle_button_event()` 再统一处理短按和长按逻辑；
- 短按推进下一步标定，长按重新开始标定。

## 3. IMU 运行时姿态融合模块

### 3.1 运行时接口

- 文件：`zed/applications/imu_runtime.h`
- 文件：`zed/applications/imu_runtime.c`

关键函数：

- `imu_runtime_reset()`
- `imu_timebase_init()`
- `imu_try_read_sample()`
- `imu_collect_gyro_bias()`
- `imu_apply_temperature_compensation()`
- `imu_should_reject_static_spike()`
- `imu_mahony_update()`

### 3.2 主要功能

#### （1）零偏采样

`imu_collect_gyro_bias()` 在系统静止时采集大量陀螺数据，计算平均零偏，作为姿态积分前的基础补偿量。

#### （2）温漂补偿

`imu_apply_temperature_compensation()` 根据温度变化修正陀螺偏置，降低长时间运行下的漂移。

#### （3）静止尖峰抑制

`imu_should_reject_static_spike()` 在静止状态下识别异常角速度尖峰，避免尖峰破坏姿态解算。

#### （4）Mahony 姿态融合

`imu_mahony_update()` 是单个 IMU 的姿态更新核心，主要功能为：

- 以陀螺积分实现快速姿态跟踪；
- 以加速度重力方向校正俯仰和横滚漂移；
- 使用比例积分项对角速度进行补偿；
- 更新四元数并做归一化。

## 4. 四元数数学工具模块

### 4.1 所在文件

- 文件：`zed/applications/imu_math.h`
- 文件：`zed/applications/imu_math.c`

### 4.2 关键函数

- `imu_quaternion_normalize()`
- `imu_quaternion_identity()`
- `imu_quaternion_multiply()`
- `imu_quaternion_inverse()`
- `imu_quaternion_rotate_vector()`
- `imu_quaternion_extract_axis_angle()`
- `imu_quaternion_extract_rotation_vector_deg()`
- `imu_quaternion_from_axis_angle_deg()`
- `imu_quaternion_angular_distance_deg()`

### 4.3 作用

该模块为整个工程提供统一的向量与四元数运算基础，包括：

- 四元数乘法与逆运算；
- 轴角提取；
- 旋转向量构造；
- 四元数旋转向量；
- 四元数姿态距离计算。

其中，`imu_quaternion_extract_rotation_vector_deg()` 是 V9.5 旧映射算法的重要基础，而 `imu_quaternion_from_axis_angle_deg()` 和 `imu_quaternion_angular_distance_deg()` 是 V9.6 前向运动学优化算法的重要基础。

## 5. 标定与姿态到关节角映射模块

### 5.1 所在文件

- 文件：`zed/applications/imu_calibration.h`
- 文件：`zed/applications/imu_calibration.c`

### 5.2 关键函数

- `imu_calibration_reset()`
- `imu_calibration_begin()`
- `imu_calibration_handle_next()`
- `imu_try_build_servo_pose()`
- `imu_capture_motion_components()`
- `imu_record_current_step()`
- `imu_finalize_upper_axis_maps()`
- `imu_finalize_lower_axis_maps()`

此外，代码中还保留了多个求解分支：

- `IMU_MAPPING_SOLVER_LEGACY_BASIS`
- `IMU_MAPPING_SOLVER_DIRECT_AXIS_V9_1`
- `IMU_MAPPING_SOLVER_FK_OPT`

当前默认配置在 `app.h` 中已切换回：

- `IMU_MAPPING_SOLVER_LEGACY_BASIS`

### 5.3 标定状态机

标定步骤定义在 `imu_app_context.h` 中，主要包括：

- `TPOSE`
- `HY+`
- `HZ-`
- `EZ+`
- `WX+`

各步骤作用如下：

- `TPOSE`：记录上下臂初始姿态，建立 `upper_offset` 与 `lower_offset`；
- `HY+`：学习肩部绕 `Y` 轴运动；
- `HZ-`：学习肩部绕 `Z` 轴运动；
- `EZ+`：学习肘部屈伸运动；
- `WX+`：学习手腕扭转运动。

### 5.4 空间姿态固连校准

`imu_capture_motion_components()` 是标定与运行态的共同核心函数，完成三件事：

- 将上臂 IMU 姿态乘以 `upper_offset`，得到上臂骨骼姿态 `upper_bone`；
- 将前臂 IMU 姿态乘以 `lower_offset`，得到前臂骨骼姿态 `lower_bone`；
- 再通过 `upper_bone` 的逆与 `lower_bone` 相乘，得到前臂相对于上臂的局部姿态 `relative_bone`。

由此实现：

- 传感器安装误差补偿；
- 前臂相对运动学解耦；
- 为肩部和肘腕关节映射提供统一输入。

### 5.5 V9.5 旧版映射方法

当求解器配置为 `IMU_MAPPING_SOLVER_LEGACY_BASIS` 时，`imu_try_build_servo_pose()` 采用旋转向量基分解方式求解：

- 先把当前姿态四元数转换为旋转向量；
- 再将当前旋转向量分解到标定阶段学习得到的两个基向量上；
- 得到肩部 `(hY, hZ)` 与前臂 `(eZ, wX)` 的分量系数；
- 最终映射为四个机械臂关节角。

该方法计算量小，实现简单，适合嵌入式实时运行。

### 5.6 V9.6 新版前向运动学优化方法

当求解器配置为 `IMU_MAPPING_SOLVER_FK_OPT` 时，系统调用：

- `imu_try_build_servo_pose_fk_opt()`
- `imu_optimize_joint_pair()`

该方法不是直接分解旋转向量，而是：

- 假设一组关节角；
- 根据标定轴和增益重建预测姿态四元数；
- 比较预测姿态与测量姿态的四元数角距离；
- 通过多尺度搜索找到误差最小的关节角组合。

该算法更符合三维刚体旋转的非线性特性，但计算量高于 V9.5。

### 5.7 输出整形

无论采用哪种映射方式，关节角输出最终都会经过统一整形：

- 限速；
- 低通滤波；
- 输出死区抑制。

这样可以减少舵机抖动和姿态跳变。

## 6. 串口协议模块

### 6.1 所在文件

- 文件：`zed/applications/imu_protocol.h`
- 文件：`zed/applications/imu_protocol.c`

### 6.2 关键函数

- `imu_protocol_send_cal_step()`
- `imu_protocol_send_cal_ok()`
- `imu_protocol_send_cal_error()`
- `imu_protocol_send_cal_done()`
- `imu_protocol_send_cal_state()`
- `imu_protocol_send_pose_frame()`
- `imu_protocol_handle_uart_commands()`

### 6.3 功能说明

该模块负责上下位机通信，主要包括两类报文：

#### （1）标定控制报文 `CAL`

支持：

- `CAL,START`
- `CAL,NEXT`
- `CAL,RESET`
- `CAL,STATUS`

用于上位机控制和查询标定状态。

#### （2）姿态输出报文 `POSE`

格式为：

`POSE,hY,hZ,eZ,wX,grip`

用于向上位机发送当前机械臂姿态角与抓手开合度。

## 7. 机械臂舵机控制模块

### 7.1 所在文件

- 文件：`zed/applications/app_arm_link.h`
- 文件：`zed/applications/app_arm_link.c`

### 7.2 关键函数

- `arm_link_init()`
- `arm_pose_calib_test()`
- `arm_apply_imu_pose_to_servos()`
- `map_shoulder_split()`

### 7.3 功能说明

该模块负责将 IMU 解算得到的四个机械臂关节量转化为底层舵机角度。

映射关系如下：

- `hY` 对应底盘舵机；
- `hZ` 对应大臂抬升，由两个舵机分段协同实现；
- `eZ` 对应肘部舵机；
- `wX` 对应手腕翻滚舵机。

其中，大臂采用分段补偿策略：

- 小角度区间由一个舵机主驱动；
- 大角度区间由另一个舵机继续补偿；

从而实现更合理的机构运动范围。

## 8. 关键数据结构

### 8.1 所在文件

- 文件：`zed/applications/imu_app_context.h`

### 8.2 核心结构体

#### `imu_runtime_t`

保存单个 IMU 的运行时状态，包括：

- 当前姿态四元数；
- 陀螺零偏；
- 温漂参数；
- Mahony 积分项；
- 时间戳；
- 数据就绪标志。

#### `imu_axis_map_t`

保存单个关节通道的学习结果，包括：

- 来源信号；
- 测量方式；
- 主运动轴；
- 参考向量；
- 增益；
- 中心角；
- 滤波与输出缓存。

#### `imu_calibration_runtime_t`

保存完整标定状态，包括：

- 上下臂偏置四元数；
- 各标定样本姿态；
- 四个关节通道映射参数；
- 当前步骤；
- 标定是否完成；
- LED 提示状态；
- 运行态辅助状态。

#### `imu_servo_pose_t`

保存最终输出的关节角结果，包括：

- `hY_deg`
- `hZ_deg`
- `eZ_deg`
- `wX_deg`
- `grip_percent`

## 9. 当前工程的软件链路总结

当前工程的软件执行链路可以概括为：

1. 上电进入 `hal_entry()`
2. 调用 `imu_test()`
3. 初始化串口、IMU、时间基和中断
4. 采集上下臂陀螺零偏
5. 进入五步引导式标定
6. 标定完成后进入实时姿态采样
7. 对 IMU 数据进行温漂补偿与 Mahony 融合
8. 计算骨骼姿态与相对姿态
9. 由姿态映射得到机械臂四个关节角
10. 输出 `POSE` 串口帧
11. 将关节角映射为 5 路舵机控制量
12. 驱动机械臂实时复现人体动作

该结构层次清晰，模块划分明确，便于后续继续扩展 EMG 融合、标定优化与上位机交互功能。
