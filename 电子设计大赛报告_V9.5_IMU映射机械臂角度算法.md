# V9.5 IMU 映射机械臂角度算法

本文给出 V9.5 版本中“空间姿态固连校准与相对运动学解算”的完整理论说明。该版本的核心思路是：先通过五步标定学习人体动作在 IMU 姿态空间中的四个代表性运动方向，再把实时姿态表示为这些基向量的线性组合，最后映射为机械臂关节角。

## 1. 系统建模与符号定义

系统使用两枚 IMU：

- 上臂 IMU：固定在大臂骨段上，用于解算肩部两自由度。
- 前臂 IMU：固定在小臂骨段上，用于解算肘部和腕部自由度。

定义：

- $q_u^W$：上臂 IMU 相对于世界坐标系的姿态四元数。
- $q_l^W$：前臂 IMU 相对于世界坐标系的姿态四元数。
- $q_{u0}^W,\ q_{l0}^W$：T-Pose 标定瞬间两枚 IMU 的姿态四元数。
- $\otimes$：四元数乘法。
- $(\cdot)^{-1}$：单位四元数的逆。

五步标定中的第 1 步为 T-Pose，将其作为人体骨段坐标系与机械臂零位姿态之间的对齐基准。因此固连补偿四元数取为

$$
q_{u,\text{off}}=(q_{u0}^W)^{-1},\qquad
q_{l,\text{off}}=(q_{l0}^W)^{-1}
$$

则实时骨段姿态为

$$
q_u = q_u^W \otimes q_{u,\text{off}},\qquad
q_l = q_l^W \otimes q_{l,\text{off}}
$$

其中 $q_u$ 对应上臂骨段姿态，$q_l$ 对应前臂骨段姿态。进一步构造前臂相对于上臂的相对姿态

$$
q_{\text{rel}} = q_u^{-1}\otimes q_l
$$

于是：

- 肩关节自由度由 $q_u$ 解算。
- 肘关节和腕关节自由度由 $q_{\text{rel}}$ 解算。

这一步实际上完成了“空间姿态固连校准”和“相对运动学解耦”：前臂姿态不再直接受肩关节整体转动影响，而是只保留相对上臂的局部运动。

## 2. 单个 IMU 的空间姿态解算

### 2.1 陀螺零偏与温漂补偿

设陀螺原始输出为 $\omega_m=[\omega_x,\omega_y,\omega_z]^T$，零偏为 $b_0$，温度漂移斜率为 $k_T$，参考温度为 $T_0$，则补偿后的角速度为

$$
\omega_c = \omega_m - \left(b_0 + k_T(T-T_0)\right)
$$

其中温漂模型采用一阶线性近似。系统在静止窗口内更新 $b_0$ 与 $k_T$，从而减小长时间积分漂移。

### 2.2 Mahony 姿态融合

加速度计测得重力方向 $a_m$，归一化后为

$$
\hat a = \frac{a_m}{\|a_m\|}
$$

由当前四元数 $q=[q_0,q_1,q_2,q_3]^T$ 计算机体坐标系下的重力预测方向

$$
\hat g =
\begin{bmatrix}
2(q_1q_3-q_0q_2) \\
2(q_0q_1+q_2q_3) \\
q_0^2-q_1^2-q_2^2+q_3^2
\end{bmatrix}
$$

姿态误差取为叉积

$$
e = \hat a \times \hat g
$$

则校正角速度为

$$
\omega = \omega_c + K_P e + K_I \int e\,dt
$$

四元数运动学方程为

$$
\dot q = \frac{1}{2} q \otimes
\begin{bmatrix}
0\\ \omega
\end{bmatrix}
$$

离散更新写为

$$
q_{k+1}=\operatorname{norm}(q_k+\dot q_k\Delta t)
$$

其中 `norm` 表示归一化。该算法兼顾了陀螺积分的快速响应和重力方向校正的长期稳定性。

## 3. 五步动作标定与固连校准

V9.5 采用五步引导式标定：

| 步骤 | 动作含义 | 目标关节 | 标准角度 |
| --- | --- | --- | --- |
| 1 | TPOSE | 记录零位偏置 | $(hY,hZ,eZ,wX)=(90,90,0,90)$ |
| 2 | HY+ | 大臂绕 $Y$ 轴正向摆动 | $(180,90,0,90)$ |
| 3 | HZ- | 大臂绕 $Z$ 轴负向摆动 | $(90,0,0,90)$ |
| 4 | EZ+ | 肘部正向弯曲 | $(90,90,90,90)$ |
| 5 | WX+ | 手腕绕前臂轴正向翻转 | $(90,90,0,180)$ |

对于每个非零动作样本，先从四元数中提取轴角表示

$$
q=\left[\cos\frac{\phi}{2},\ \mathbf n\sin\frac{\phi}{2}\right]
$$

其中 $\mathbf n$ 为单位转轴，$\phi$ 为旋转角。于是每个关节通道学习到：

- 一个主运动轴 $\mathbf n_i$；
- 一个原始响应角 $\phi_i$；
- 一个比例增益 $g_i$。

令目标关节动作幅度均为 $\Delta\theta=90^\circ$，则各通道的增益写为

$$
g_i=\frac{\Delta\theta_i}{\phi_i}
$$

其中：

- $hY$ 通道：$\Delta\theta_{hY}=+90^\circ$
- $hZ$ 通道：$\Delta\theta_{hZ}=-90^\circ$
- $eZ$ 通道：$\Delta\theta_{eZ}=+90^\circ$
- $wX$ 通道：$\Delta\theta_{wX}=+90^\circ$

对应的中心角分别为

$$
\theta_{c,hY}=90^\circ,\quad
\theta_{c,hZ}=90^\circ,\quad
\theta_{c,eZ}=0^\circ,\quad
\theta_{c,wX}=90^\circ
$$

## 4. 运动分量测量模型

### 4.1 Swing 摆动角测量

对于肩部和肘部这类“摆动”自由度，采用参考向量在垂直于目标轴平面上的投影角来测量。

给定参考向量 $\mathbf r$ 与目标轴 $\mathbf n$，先做投影：

$$
\mathbf r_\perp = \mathbf r-(\mathbf r\cdot \mathbf n)\mathbf n
$$

将当前姿态作用到参考向量上：

$$
\mathbf r' = R(q)\mathbf r
$$

同样投影得

$$
\mathbf r'_\perp = \mathbf r'-(\mathbf r'\cdot \mathbf n)\mathbf n
$$

归一化后，摆动角定义为

$$
\alpha_{\text{swing}}=
\operatorname{atan2}\left(
\mathbf n\cdot(\mathbf r_\perp\times \mathbf r'_\perp),
\mathbf r_\perp\cdot \mathbf r'_\perp
\right)
$$

### 4.2 Twist 扭转角测量

对于腕部绕前臂轴的旋转，不能直接用普通摆角，而要从四元数中抽取绕目标轴 $\mathbf n$ 的扭转分量。设

$$
q=[q_0,\mathbf q_v],\qquad \mathbf q_v=[q_1,q_2,q_3]^T
$$

将虚部向目标轴投影：

$$
p=\mathbf q_v\cdot \mathbf n
$$

构造纯扭转四元数

$$
q_{\text{twist}}=[q_0,\ p\mathbf n]
$$

归一化后，其对应扭转角为

$$
\alpha_{\text{twist}}=
2\operatorname{atan2}\left(\|p\mathbf n\|,\ q_0\right)
$$

若 $p<0$，则取负号以保持旋向一致。

## 5. V9.5 的相对运动学解算方法

### 5.1 旋转向量表示

V9.5 的关键思想是把姿态四元数转成旋转向量，再做二维线性分解。若

$$
q=\left[\cos\frac{\phi}{2},\ \mathbf n\sin\frac{\phi}{2}\right]
$$

则旋转向量定义为

$$
\mathbf v = \phi \mathbf n
$$

于是：

- 肩部实时姿态对应旋转向量 $\mathbf v_u$；
- 标定时 $HY+$、$HZ-$ 两个样本分别对应 $\mathbf v_{hY},\mathbf v_{hZ}$；
- 前臂相对姿态对应旋转向量 $\mathbf v_{\text{rel}}$；
- 标定时 $EZ+$、$WX+$ 两个样本分别对应 $\mathbf v_{eZ},\mathbf v_{wX}$。

### 5.2 二维基向量分解

上臂姿态近似表示为两个标定基向量的线性组合：

$$
\mathbf v_u \approx c_{hY}\mathbf v_{hY}+c_{hZ}\mathbf v_{hZ}
$$

前臂相对姿态近似表示为

$$
\mathbf v_{\text{rel}} \approx c_{eZ}\mathbf v_{eZ}+c_{wX}\mathbf v_{wX}
$$

将问题写成最小二乘线性方程。以肩部为例：

$$
\begin{bmatrix}
\mathbf v_{hY}\cdot \mathbf v_{hY} & \mathbf v_{hY}\cdot \mathbf v_{hZ} \\
\mathbf v_{hY}\cdot \mathbf v_{hZ} & \mathbf v_{hZ}\cdot \mathbf v_{hZ}
\end{bmatrix}
\begin{bmatrix}
c_{hY}\\
c_{hZ}
\end{bmatrix}
=
\begin{bmatrix}
\mathbf v_u\cdot \mathbf v_{hY}\\
\mathbf v_u\cdot \mathbf v_{hZ}
\end{bmatrix}
$$

若行列式

$$
\Delta=a_{11}a_{22}-a_{12}^2
$$

不为零，则解得

$$
c_{hY}=\frac{b_1a_{22}-b_2a_{12}}{\Delta},\qquad
c_{hZ}=\frac{a_{11}b_2-a_{12}b_1}{\Delta}
$$

同理可解得 $c_{eZ},c_{wX}$。

### 5.3 关节角计算

V9.5 将系数直接映射为关节角增量：

$$
\begin{aligned}
\theta_{hY} &= 90^\circ + 90^\circ \cdot c_{hY} \\
\theta_{hZ} &= 90^\circ - 90^\circ \cdot c_{hZ} \\
\theta_{eZ} &= 0^\circ  + 90^\circ \cdot c_{eZ} \\
\theta_{wX} &= 90^\circ + 90^\circ \cdot c_{wX}
\end{aligned}
$$

再将结果限制在 $[0^\circ,180^\circ]$ 内。

### 5.4 符号消歧

由于四元数轴角表示存在方向二义性，V9.5 对每对关节轴都进行正负组合遍历。以上臂为例，设候选轴为

$$
\tilde{\mathbf n}_{hY}\in\{\mathbf n_{hY},-\mathbf n_{hY}\},\qquad
\tilde{\mathbf n}_{hZ}\in\{\mathbf n_{hZ},-\mathbf n_{hZ}\}
$$

构造参考向量

$$
\mathbf r = \frac{\tilde{\mathbf n}_{hY}\times \tilde{\mathbf n}_{hZ}}
{\|\tilde{\mathbf n}_{hY}\times \tilde{\mathbf n}_{hZ}\|}
$$

计算在两个标定动作下的主响应角，并以

$$
J_{\text{upper}}=
\left|90^\circ-\alpha_{hY}\right|+
\left|-90^\circ-\alpha_{hZ}\right|
$$

为代价，选取最小者作为最终上臂映射。同理，下臂映射使用

$$
J_{\text{lower}}=
\left|90^\circ-\alpha_{eZ}\right|+
\left|90^\circ-\alpha_{wX}\right|
$$

进行选择。

## 6. 输出平滑与抗抖动

为抑制 IMU 噪声和姿态跳变，V9.5 在关节角输出端加入三层整形。

### 6.1 限速

设当前原始关节增量为 $x_k$，上一时刻为 $x_{k-1}$，则

$$
\Delta x_k = \operatorname{clip}(x_k-x_{k-1},\ -2.4^\circ,\ 2.4^\circ)
$$

$$
\tilde x_k = x_{k-1}+\Delta x_k
$$

### 6.2 一阶低通滤波

$$
\bar x_k = \bar x_{k-1} + \alpha(\tilde x_k-\bar x_{k-1}),\qquad \alpha=0.20
$$

### 6.3 输出死区

若本次整数角度与上一输出角满足

$$
|\theta_k-\theta_{k-1}|\le 1^\circ
$$

则保持上一输出不变，以减小小幅抖动。

## 7. 机械臂舵机角映射

V9.5 解算得到的四个关节角为：

- $hY$：大臂绕 $Y$ 轴角度，对应机械臂底盘旋转；
- $hZ$：大臂绕 $Z$ 轴角度，对应机械臂大臂抬升；
- $eZ$：肘部绕 $Z$ 轴角度，对应机械臂小臂抬升；
- $wX$：手腕绕 $X$ 轴角度，对应机械腕翻滚。

再映射到 5 路舵机：

### 7.1 底盘舵机

$$
\theta_{S5}=hY
$$

### 7.2 大臂分段映射

机械臂大臂由两个舵机协同驱动，分界点设为 $80^\circ$。

当 $0^\circ\le hZ\le 80^\circ$ 时：

$$
\theta_{S4}=90^\circ-hZ,\qquad
\theta_{S3}=90^\circ
$$

当 $80^\circ<hZ\le 180^\circ$ 时：

$$
\theta_{S4}=10^\circ,\qquad
\theta_{S3}=90^\circ+(hZ-80^\circ)
$$

### 7.3 肘部舵机

$$
\theta_{S2}=90^\circ-eZ
$$

### 7.4 腕部舵机

$$
\theta_{S1}=180^\circ-wX
$$

若舵机存在安装反向和零位偏置，则统一写成

$$
\theta_{\text{out}}=
\begin{cases}
\theta+\delta, & \text{同向安装}\\
180^\circ-\theta+\delta, & \text{反向安装}
\end{cases}
$$

最后所有舵机命令均限制在 $[0^\circ,180^\circ]$ 内。

## 8. V9.5 算法特点总结

V9.5 的优点是结构清晰、计算量小、实现简单，能够在两枚 IMU 和 5 步标定条件下完成四个机械臂自由度的实时解算。其本质是“姿态向量投影法”，即把复杂的人体空间姿态变化投影到两个二维子空间：

- 上臂二维子空间：$HY$ 与 $HZ$；
- 前臂二维子空间：$EZ$ 与 $WX$。

该方法适合实时嵌入式实现，但由于采用线性分解，默认不同自由度之间近似可叠加，因此在大角度耦合动作下会引入一定非线性误差。这也是后续 V9.6 版本进一步升级为前向运动学优化求解的直接原因。
