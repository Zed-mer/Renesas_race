## 2.3.3 空间姿态固连校准与相对运动学解算

为解决 IMU 在人体骨段上的随机佩戴误差以及传感器局部坐标系与人体骨骼坐标系不一致的问题，系统设计了基于 T-Pose 的空间姿态固连校准方法。在标定开始阶段，要求操作者保持标准 T-Pose 姿态，此时系统分别记录上臂 IMU 与前臂 IMU 的初始姿态四元数，并将其逆四元数作为安装偏置参数，即上臂偏置四元数 `q_upper_offset` 和前臂偏置四元数 `q_lower_offset`。在实时工作过程中，单片机将实时测得的 IMU 绝对姿态与对应偏置参数进行四元数乘法，从而把“传感器姿态”统一转换为“骨骼姿态”，实现安装误差补偿。其计算关系为：

q_upper_bone = q_upper ⊗ q_upper_offset

q_lower_bone = q_lower ⊗ q_lower_offset

其中，`q_upper` 和 `q_lower` 分别表示两枚 IMU 当前相对于世界坐标系的姿态四元数，`⊗` 表示四元数乘法。经过该步骤后，系统获得了与人体真实运动一致的上臂骨段姿态 `q_upper_bone` 和前臂骨段姿态 `q_lower_bone`，从而消除了传感器贴附角度、绑带松紧和安装方向随机性带来的初始偏差。

为进一步提取肘关节和手腕相对于上臂的局部运动信息，系统采用相对运动学解算方法。由于前臂的绝对姿态同时叠加了肩关节整体转动和前臂局部转动，如果直接使用绝对姿态进行关节解算，会导致肘部和手腕运动与肩部运动发生耦合。为此，算法对上臂骨段姿态求逆，再与前臂骨段姿态相乘，得到前臂相对于上臂的相对姿态四元数 `q_relative`：

q_relative = q_upper_bone^(-1) ⊗ q_lower_bone

式中，`q_upper_bone^(-1)` 为上臂骨段姿态四元数的逆四元数。该运算的物理意义在于：先利用上臂姿态建立局部参考坐标系，再将前臂姿态投影到该局部坐标系中，从而剥离肩关节整体转动的影响，仅保留肘部屈伸和腕部旋转等相对运动成分。由此得到的 `q_relative` 能够准确反映前臂相对于大臂的局部空间姿态，是后续关节角解算的关键输入量。

与传统直接采用欧拉角分解的方法不同，本系统结合五步标定过程中学习得到的主运动轴信息，对 `q_upper_bone` 和 `q_relative` 进行关节空间求解。对于肩部自由度，系统利用上臂姿态解算大臂绕 `Y` 轴和 `Z` 轴的运动分量；对于前臂自由度，系统利用相对姿态解算肘部绕 `Z` 轴的屈伸运动以及手腕绕前臂主轴的扭转运动。V9.6 版本进一步引入前向运动学优化方法：先根据候选关节角重建预测姿态四元数，再与实时测量姿态比较，选取姿态误差最小的一组关节角作为最终输出。其优化目标可表示为：

min d(q_pred, q_meas)

其中，`q_pred` 为由候选关节角重建得到的预测姿态四元数，`q_meas` 为实时测得的骨骼姿态四元数，`d(·)` 表示两四元数之间的姿态角距离。通过该方法，系统避免了大角度复合动作下欧拉角分解带来的奇异性与耦合误差，提高了关节角解算的稳定性与准确性。

最终，经固连校准和相对运动学解算后，系统能够稳定获得机械臂控制所需的四个核心关节量，即底盘旋转角 `hY`、大臂抬升角 `hZ`、肘部屈伸角 `eZ` 和手腕翻滚角 `wX`。这些关节量再经过限幅、滤波和舵机映射后输出给机械臂执行机构，从而实现人体动作到机械臂动作的平滑、准确复现。
