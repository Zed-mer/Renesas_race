# V9.6 IMU 映射机械臂角度算法

本文给出 V9.6 版本中“空间姿态固连校准与相对运动学解算”的完整理论说明。与 V9.5 的旋转向量线性分解不同，V9.6 的核心改进是：保留五步标定学习到的运动轴与比例系数，但在运行阶段不再直接求线性系数，而是通过前向运动学重建姿态，并以姿态误差最小为目标搜索最优关节角。

## 1. 系统建模与基本思路

系统同样使用两枚 IMU：

- 上臂 IMU 用于解算肩部两自由度；
- 前臂 IMU 用于解算肘部和腕部两自由度。

定义：

- $q_u^W$：上臂 IMU 相对于世界坐标系的姿态四元数；
- $q_l^W$：前臂 IMU 相对于世界坐标系的姿态四元数；
- $q_{u0}^W,\ q_{l0}^W$：T-Pose 标定时刻姿态；
- $\otimes$：四元数乘法。

T-Pose 固连校准与相对姿态解算方式与 V9.5 相同：

$$
q_{u,\text{off}}=(q_{u0}^W)^{-1},\qquad
q_{l,\text{off}}=(q_{l0}^W)^{-1}
$$

$$
q_u=q_u^W\otimes q_{u,\text{off}},\qquad
q_l=q_l^W\otimes q_{l,\text{off}}
$$

$$
q_{\text{rel}}=q_u^{-1}\otimes q_l
$$

因此：

- $q_u$ 表示上臂骨段的绝对姿态；
- $q_{\text{rel}}$ 表示前臂相对于上臂的局部姿态。

这一构造把肩部整体运动从肘腕解算中分离出去，属于典型的相对运动学建模。

## 2. 单个 IMU 的空间姿态解算

V9.6 沿用 V9.5 的 Mahony 融合框架和温漂补偿策略。

### 2.1 陀螺补偿模型

$$
\omega_c = \omega_m - \left(b_0 + k_T(T-T_0)\right)
$$

其中 $b_0$ 为零偏，$k_T$ 为温漂斜率，$T_0$ 为参考温度。

### 2.2 Mahony 姿态融合

归一化加速度向量为

$$
\hat a=\frac{a_m}{\|a_m\|}
$$

由四元数 $q=[q_0,q_1,q_2,q_3]^T$ 预测重力方向

$$
\hat g =
\begin{bmatrix}
2(q_1q_3-q_0q_2) \\
2(q_0q_1+q_2q_3) \\
q_0^2-q_1^2-q_2^2+q_3^2
\end{bmatrix}
$$

姿态误差：

$$
e=\hat a\times \hat g
$$

校正角速度：

$$
\omega=\omega_c+K_Pe+K_I\int e\,dt
$$

四元数更新：

$$
\dot q=\frac{1}{2}q\otimes
\begin{bmatrix}
0\\\omega
\end{bmatrix},
\qquad
q_{k+1}=\operatorname{norm}(q_k+\dot q_k\Delta t)
$$

该部分的作用是稳定输出两枚 IMU 的空间姿态，为后续关节角求解提供可靠输入。

## 3. 五步固连校准与动作样本采集

V9.6 仍采用五步引导式标定：

| 步骤 | 动作 | 目标关节 | 参考角度 |
| --- | --- | --- | --- |
| 1 | TPOSE | 记录零位偏置 | $(90,90,0,90)$ |
| 2 | HY+ | 肩部 $Y$ 轴正向摆动 | $(180,90,0,90)$ |
| 3 | HZ- | 肩部 $Z$ 轴负向摆动 | $(90,0,0,90)$ |
| 4 | EZ+ | 肘部正向弯曲 | $(90,90,90,90)$ |
| 5 | WX+ | 手腕正向翻滚 | $(90,90,0,180)$ |

对每个动作样本，从四元数中提取轴角表示

$$
q=\left[\cos\frac{\phi}{2},\ \mathbf n\sin\frac{\phi}{2}\right]
$$

得到每个自由度对应的：

- 主运动轴 $\mathbf n_i$；
- 原始动作角 $\phi_i$；
- 角度缩放增益 $g_i$。

令标定目标动作幅度为 $\Delta\theta_i$，则

$$
g_i=\frac{\Delta\theta_i}{\phi_i}
$$

各中心角仍为

$$
\theta_{c,hY}=90^\circ,\quad
\theta_{c,hZ}=90^\circ,\quad
\theta_{c,eZ}=0^\circ,\quad
\theta_{c,wX}=90^\circ
$$

## 4. 运动分量测量模型

### 4.1 Swing 摆动角

给定参考向量 $\mathbf r$ 与目标轴 $\mathbf n$，定义其在垂直于 $\mathbf n$ 平面上的投影：

$$
\mathbf r_\perp=\mathbf r-(\mathbf r\cdot\mathbf n)\mathbf n
$$

当前姿态作用后的向量为

$$
\mathbf r' = R(q)\mathbf r
$$

投影后得到

$$
\mathbf r'_\perp=\mathbf r'-(\mathbf r'\cdot\mathbf n)\mathbf n
$$

于是摆动角为

$$
\alpha_{\text{swing}}=
\operatorname{atan2}\left(
\mathbf n\cdot(\mathbf r_\perp\times\mathbf r'_\perp),
\mathbf r_\perp\cdot\mathbf r'_\perp
\right)
$$

### 4.2 Twist 扭转角

若四元数写成

$$
q=[q_0,\mathbf q_v]
$$

则其沿目标轴 $\mathbf n$ 的扭转分量由投影

$$
p=\mathbf q_v\cdot \mathbf n
$$

构造：

$$
q_{\text{twist}}=[q_0,\ p\mathbf n]
$$

归一化后，扭转角为

$$
\alpha_{\text{twist}}=
2\operatorname{atan2}\left(\|p\mathbf n\|,\ q_0\right)
$$

若 $p<0$，则取负号以保留旋向。

## 5. V9.6 的标定质量判据

V9.6 的第一个改进是：不再只根据“主响应最接近目标”来选取关节轴符号，而是额外加入标定质量评价，避免两个自由度不够正交或串扰过大时误选轴向。

对任意一对候选轴 $\mathbf n_1,\mathbf n_2$，定义：

### 5.1 基向量可分离性

$$
D=(\mathbf n_1\cdot\mathbf n_1)(\mathbf n_2\cdot\mathbf n_2)-(\mathbf n_1\cdot\mathbf n_2)^2
$$

若 $D$ 过小，说明两轴接近平行，后续双自由度求解将病态。V9.6 要求

$$
D \ge 0.55
$$

### 5.2 轴夹角约束

$$
\beta=\arccos(\mathbf n_1\cdot\mathbf n_2)
$$

要求

$$
\beta \ge 50^\circ
$$

### 5.3 主响应幅度约束

若两次标准动作应分别产生目标响应 $\alpha_1^\star,\alpha_2^\star$，则要求

$$
|\alpha_1-\alpha_1^\star|\le 25^\circ,\qquad
|\alpha_2-\alpha_2^\star|\le 25^\circ
$$

### 5.4 串扰约束

设第 1 个动作在第 2 个通道上的泄漏为 $c_1$，第 2 个动作在第 1 个通道上的泄漏为 $c_2$，则要求

$$
|c_1|\le 20^\circ,\qquad |c_2|\le 20^\circ
$$

仅当上述全部条件成立时，该组候选轴才被接受为有效标定结果。这样可以显著提高标定的可辨识性和运行时稳定性。

## 6. V9.6 的前向运动学优化解算

### 6.1 由关节角反推姿态

V9.6 不再直接把姿态分解成线性系数，而是先假设某一组关节角，再由该组关节角重建预测姿态。

设某一对关节的目标角为 $(\theta_1,\theta_2)$，其中心角分别为 $(\theta_{c1},\theta_{c2})$，比例增益分别为 $(g_1,g_2)$，则对应的原始转角为

$$
\phi_1=\frac{\theta_1-\theta_{c1}}{g_1},\qquad
\phi_2=\frac{\theta_2-\theta_{c2}}{g_2}
$$

由轴角生成单轴四元数

$$
q_1=\left[\cos\frac{\phi_1}{2},\ \mathbf n_1\sin\frac{\phi_1}{2}\right],\qquad
q_2=\left[\cos\frac{\phi_2}{2},\ \mathbf n_2\sin\frac{\phi_2}{2}\right]
$$

于是可构造预测姿态

$$
\hat q(\theta_1,\theta_2)=q_1\otimes q_2
$$

其中：

- 对上臂使用 $(\theta_{hY},\theta_{hZ})$ 生成 $\hat q_u$；
- 对前臂使用 $(\theta_{eZ},\theta_{wX})$ 生成 $\hat q_{\text{rel}}$。

### 6.2 姿态误差目标函数

设目标姿态为 $q_t$，预测姿态为 $\hat q$，则姿态误差定义为两四元数之间的最小旋转角：

$$
q_\Delta=\hat q^{-1}\otimes q_t
$$

若

$$
q_\Delta=\left[\cos\frac{\varepsilon}{2},\ \mathbf n_\Delta\sin\frac{\varepsilon}{2}\right]
$$

则代价函数为

$$
J(\theta_1,\theta_2)=\varepsilon
$$

也即：

$$
J(\theta_1,\theta_2)=d\!\left(\hat q(\theta_1,\theta_2),q_t\right)
$$

V9.6 的关节角求解就转化为如下优化问题：

$$
(\theta_1^\star,\theta_2^\star)=
\arg\min_{\theta_1,\theta_2\in[0^\circ,180^\circ]} J(\theta_1,\theta_2)
$$

### 6.3 分层搜索策略

为兼顾实时性和精度，V9.6 采用“粗搜 + 局部细化”的层级优化方法。

#### 1. 热启动

若上一帧已有最优解，则用上一帧结果作为当前初值：

$$
\theta_1^{(0)}=\theta_{1,k-1}^\star,\qquad
\theta_2^{(0)}=\theta_{2,k-1}^\star
$$

这样可利用人体运动连续性降低计算量。

#### 2. 粗网格重启

若热启动点的代价超过阈值 $2^\circ$，说明上一帧解已经不可靠，则在

$$
\{0^\circ,45^\circ,90^\circ,135^\circ,180^\circ\}^2
$$

上进行粗网格搜索，选取代价最小者作为新初值。

#### 3. 多尺度局部搜索

随后依次采用步长

$$
30^\circ,\ 12^\circ,\ 4^\circ,\ 1^\circ,\ 0.25^\circ
$$

对邻域进行爬山搜索。若某一邻域点使代价下降，则接受该点，并继续搜索，直到当前步长下不存在更优邻点为止。

这种方法等价于对二维目标函数做逐级逼近，能够在较低计算量下得到高精度近似最优解。

### 6.4 上臂与前臂的分别求解

V9.6 将四自由度拆成两个二维优化问题：

$$
(\theta_{hY}^\star,\theta_{hZ}^\star)=
\arg\min d\!\left(\hat q_u(\theta_{hY},\theta_{hZ}), q_u\right)
$$

$$
(\theta_{eZ}^\star,\theta_{wX}^\star)=
\arg\min d\!\left(\hat q_{\text{rel}}(\theta_{eZ},\theta_{wX}), q_{\text{rel}}\right)
$$

这样既保持了相对运动学解耦，又避免了四维联合搜索的高复杂度。

## 7. V9.6 的输出整形

V9.6 的第二个改进是：输出整形与求解过程解耦。优化器先给出目标关节角 $\theta_i^\star$，再统一经过输出层整形。

### 7.1 角速度限幅

设本帧目标增量为 $x_k=\theta_k^\star-\theta_c$，上一帧为 $x_{k-1}$，则

$$
\Delta x_k=\operatorname{clip}(x_k-x_{k-1},-2.4^\circ,2.4^\circ)
$$

$$
\tilde x_k=x_{k-1}+\Delta x_k
$$

### 7.2 一阶低通滤波

$$
\bar x_k=\bar x_{k-1}+0.20(\tilde x_k-\bar x_{k-1})
$$

### 7.3 输出死区

若

$$
|\theta_k-\theta_{k-1}|\le 1^\circ
$$

则维持上一输出角，以抑制小范围抖动。

与 V9.5 不同，V9.6 的平滑对象不再是“基分解得到的线性系数”，而是“优化得到的真实关节目标角”，因此物理意义更直接。

## 8. 机械臂舵机映射

V9.6 的机械臂舵机映射关系与 V9.5 保持一致，便于下位机控制接口复用。

### 8.1 底盘舵机

$$
\theta_{S5}=hY
$$

### 8.2 大臂双舵机分段映射

分界角为 $80^\circ$。

当 $0^\circ\le hZ\le 80^\circ$ 时：

$$
\theta_{S4}=90^\circ-hZ,\qquad
\theta_{S3}=90^\circ
$$

当 $80^\circ<hZ\le 180^\circ$ 时：

$$
\theta_{S4}=10^\circ,\qquad
\theta_{S3}=90^\circ+(hZ-80^\circ)
$$

### 8.3 肘部舵机

$$
\theta_{S2}=90^\circ-eZ
$$

### 8.4 腕部舵机

$$
\theta_{S1}=180^\circ-wX
$$

若考虑安装方向和零位偏置，则统一写成

$$
\theta_{\text{out}}=
\begin{cases}
\theta+\delta, & \text{同向安装}\\
180^\circ-\theta+\delta, & \text{反向安装}
\end{cases}
$$

最后限制在 $[0^\circ,180^\circ]$ 范围内。

## 9. V9.6 相对 V9.5 的改进总结

V9.6 的主要改进可以概括为两点。

### 9.1 标定阶段更可靠

V9.5 只依据主响应角与目标动作的接近程度选取轴向；V9.6 增加了：

- 基向量行列式判据；
- 轴间分离角判据；
- 主响应误差判据；
- 串扰约束判据。

因此 V9.6 对“动作不标准”或“安装姿态不理想”的容错性更好。

### 9.2 运行阶段更符合非线性运动学本质

V9.5 采用

$$
\mathbf v \approx c_1\mathbf v_1+c_2\mathbf v_2
$$

的线性近似，默认两个关节自由度在旋转向量空间内近似可加；而 V9.6 直接在四元数姿态空间中最小化

$$
d(\hat q,q_t)
$$

因此更符合三维刚体旋转的非线性特性。在大角度、复合动作和轴间耦合较明显时，V9.6 的求解精度与稳定性都优于 V9.5。

## 10. 结论

V9.6 本质上是一种“基于标定轴学习的前向运动学反求算法”。其工作链路可以概括为：

1. 用两枚 IMU 经 Mahony 融合得到上臂和前臂空间姿态。
2. 通过 T-Pose 完成 IMU 与骨段的固连校准。
3. 通过相对姿态 $q_{\text{rel}}=q_u^{-1}\otimes q_l$ 完成前臂相对运动学解耦。
4. 用五步动作学习四个关节通道的主运动轴和比例系数。
5. 用前向运动学重建姿态，并通过姿态误差最小原则搜索最优关节角。
6. 将关节角进一步映射到 5 路舵机控制量。

因此，V9.6 不仅完成了空间姿态固连校准，也通过相对运动学和优化求解实现了从人体 IMU 姿态到机械臂角度的稳定、高精度映射。
