# 串口协议说明

本项目通过 `UART7` 与上位机通信，串口参数固定如下：

- 波特率：`115200`
- 数据位：`8`
- 停止位：`1`
- 校验位：`None`
- 流控：`None`

串口采用单行文本协议，支持两类报文：

- `POSE`：下位机主动上报实时姿态
- `CAL`：上位机与下位机双向交互的标定控制/状态报文

每一行都建议使用 `ASCII` 或 `UTF-8` 编码，结尾可为 `\n` 或 `\r\n`。

## `POSE` 实时姿态报文

格式：

```text
POSE,<hY>,<hZ>,<eZ>,<wX>,<grip>
```

示例：

```text
POSE,90,120,45,135,60
POSE,100,110,30,90,20
POSE,999,-5,45,135,200
```

字段定义：

| 字段 | 含义 | 合法范围 | 页面作用 |
| --- | --- | --- | --- |
| `hY` | 大臂绕 Y 轴角度 | `0-180` | 映射到底盘旋转 |
| `hZ` | 大臂绕 Z 轴角度 | `0-180` | 映射到机械臂大臂抬升 |
| `eZ` | 肘部绕 Z 轴角度 | `0-180` | 映射到机械臂小臂抬升 |
| `wX` | 手腕绕 X 轴角度 | `0-180` | 映射到机械手腕翻滚 |
| `grip` | 抓手开合程度 | `0-100` | 映射到手爪开合 |

解析规则：

- 空行直接忽略
- 只有前缀为 `POSE` 的 6 字段报文才会被当作姿态报文
- 任一字段不是整数时，该帧丢弃
- 任一字段超出范围时，接收端应先钳制再使用
- 页面始终以“最近一帧有效 `POSE`”作为当前实时姿态

例如：

```text
POSE,999,-5,45,135,200
```

接收后应按下列值生效：

```text
POSE,180,0,45,135,100
```

推荐发送频率：

- 推荐 `20-50 Hz`
- 手工演示时 `10 Hz` 也可正常观察

## `CAL` 标定控制报文

`CAL` 是动作标定阶段使用的控制协议。标定时按键与上位机共用同一套状态机，因此：

- 下位机短按按键，等效于上位机发送 `CAL,NEXT`
- 下位机长按 `2 秒`，等效于上位机发送 `CAL,RESET`
- 上位机可随时发送 `CAL,STATUS` 查询当前步骤

### 上位机发送给下位机

```text
CAL,START
CAL,NEXT
CAL,RESET
CAL,STATUS
```

命令定义：

| 命令 | 含义 |
| --- | --- |
| `CAL,START` | 清空旧标定并进入 `STEP1 TPOSE` |
| `CAL,NEXT` | 记录当前步骤动作；成功则进入下一步 |
| `CAL,RESET` | 清空全部标定并回到 `STEP1 TPOSE` |
| `CAL,STATUS` | 查询当前标定状态 |

### 下位机返回给上位机

#### 当前等待步骤

```text
CAL,STEP,<idx>,<name>
```

示例：

```text
CAL,STEP,1,TPOSE
CAL,STEP,4,EZ+
```

#### 单步记录成功

```text
CAL,OK,<idx>,<name>
```

示例：

```text
CAL,OK,2,HY+
```

#### 单步记录失败

```text
CAL,ERR,<code>,<name>
```

示例：

```text
CAL,ERR,WEAK,HY+
CAL,ERR,AMBIG,EZ+
CAL,ERR,NODATA,TPOSE
```

失败码说明：

| 失败码 | 含义 |
| --- | --- |
| `WEAK` | 主响应轴太弱，动作幅度不够 |
| `AMBIG` | 多个轴同时响应，当前动作不够标准 |
| `NODATA` | 当前没有足够的 IMU 数据可用 |

#### 全部标定完成

```text
CAL,DONE
```

当收到 `CAL,DONE` 后，下位机会开始恢复周期性输出 `POSE`。

#### 当前状态查询返回

```text
CAL,STATE,<idx>,<name>,<calibrated>
```

示例：

```text
CAL,STATE,1,TPOSE,0
CAL,STATE,4,EZ+,0
CAL,STATE,5,DONE,1
```

字段说明：

- `idx`：当前步骤编号，从 `1` 开始
- `name`：当前步骤名，可能为 `TPOSE` `HY+` `HZ-` `EZ+` `WX+` `DONE`
- `calibrated`：`0` 表示尚未完成，`1` 表示已经完成

## 五步标准动作

| 步骤 | 名称 | 标准动作 | 提示姿态 |
| --- | --- | --- | --- |
| `1` | `TPOSE` | 向前伸直，掌心朝内 | `POSE,90,90,0,90,0` |
| `2` | `HY+` | 向上伸直，掌心朝内 | `POSE,180,90,0,90,0` |
| `3` | `HZ-` | 向右伸直，掌心朝前 | `POSE,90,0,0,90,0` |
| `4` | `EZ+` | 大臂往前，小臂和大臂呈直角，掌心朝自己 | `POSE,90,90,90,90,0` |
| `5` | `WX+` | 向前伸直，掌心朝下 | `POSE,90,90,0,180,0` |

## 报文混发规则

- 标定未完成时，下位机主要输出 `CAL,...` 状态行，不输出实时 `POSE`
- 标定完成后，下位机恢复输出 `POSE,...`
- 上位机在任何时刻都可以发送 `CAL,STATUS`
- 上位机解析时应将 `POSE` 当成实时姿态，将 `CAL` 当成控制/状态，不要混用

## 最小发送示例

### Arduino / 单片机发送 `POSE`

```cpp
void setup() {
  Serial.begin(115200);
}

void loop() {
  Serial.println("POSE,90,120,45,135,60");
  delay(50);  // 20Hz
}
```

### Python 上位机发送 `CAL`

```python
import serial
import time

ser = serial.Serial("COM3", 115200, timeout=1)

ser.write(b"CAL,START\n")
time.sleep(0.1)
ser.write(b"CAL,STATUS\n")
```

## 使用提示

- 页面需要运行在支持 Web Serial API 的浏览器中，建议使用最新版 Chrome 或 Edge
- 首次连接时，需要点击页面上的“连接串口”按钮并手动选择串口设备
- 如果页面提示安全上下文问题，请通过 `localhost` 或 `https` 打开页面
