# 串口协议说明

本页面通过电脑串口接收姿态数据，串口参数固定如下：

- 波特率：`115200`
- 数据位：`8`
- 停止位：`1`
- 校验位：`None`
- 流控：`None`

## 报文格式

每一帧使用一行文本，编码建议为 `ASCII` 或 `UTF-8`，以换行结束：

```text
POSE,<hY>,<hZ>,<eZ>,<wX>,<grip>
```

示例：

```text
POSE,90,120,45,135,60
POSE,100,110,30,90,20
POSE,999,-5,45,135,200
```

说明：

- 帧尾可以使用 `\n` 或 `\r\n`
- 接收端会忽略空行
- 接收端只认前缀 `POSE`
- 字段数量必须正好 6 个

## 字段定义

| 字段 | 含义 | 合法范围 | 页面作用 |
| --- | --- | --- | --- |
| `hY` | 大臂绕 Y 轴角度 | `0-180` | 映射到底盘转动 |
| `hZ` | 大臂绕 Z 轴角度 | `0-180` | 映射到机械臂大臂抬升 |
| `eZ` | 肘部绕 Z 轴角度 | `0-180` | 映射到机械臂小臂抬升 |
| `wX` | 手腕绕 X 轴角度 | `0-180` | 映射到机械手翻滚 |
| `grip` | 手爪张合程度 | `0-100` | 映射到手爪开合 |

## 解析规则

- 空行直接忽略。
- 如果前缀不是 `POSE`，整帧丢弃。
- 如果字段数不对，整帧丢弃。
- 如果任一字段不是整数，整帧丢弃。
- 如果数值超出范围，接收端会自动钳制到合法范围再使用。
- 页面始终以“最近一帧有效报文”为当前姿态。

例如：

```text
POSE,999,-5,45,135,200
```

接收后会按下面的值生效：

```text
POSE,180,0,45,135,100
```

## 建议发送频率

- 推荐 `20-50 Hz`
- 如果只是手动演示，`10 Hz` 也可以正常观察

## 最小发送示例

### Arduino / 单片机

```cpp
void setup() {
  Serial.begin(115200);
}

void loop() {
  Serial.println("POSE,90,120,45,135,60");
  delay(50);  // 20Hz
}
```

### Python 上位机

```python
import serial
import time

ser = serial.Serial("COM3", 115200, timeout=1)

while True:
    ser.write(b"POSE,90,120,45,135,60\n")
    time.sleep(0.05)  # 20Hz
```

## 使用提示

- 页面需要运行在支持 Web Serial API 的浏览器中，建议使用最新版 Chrome 或 Edge。
- 首次连接时，需要点击页面上的“连接串口”按钮并手动选择串口设备。
- 如果页面提示安全上下文问题，请通过 `localhost` 或 `https` 打开页面，而不要直接使用不安全来源。
